import { usePrototype } from '../context/PrototypeContext';

export default function MyDay() {
  const { tasks, completeTask } = usePrototype(); const remaining = tasks.filter((task) => !task.done);
  return <section className="p-4 md:p-8 max-w-6xl mx-auto space-y-6"><div><p className="text-primary font-semibold text-sm">Advisor workspace</p><h2 className="text-3xl font-bold">My Day</h2><p className="text-on-surface-variant mt-1">A focused list of customer follow-ups and priority work.</p></div><section className="grid md:grid-cols-3 gap-4"><div className="bg-white rounded-xl border border-outline-variant p-5"><p className="text-sm text-on-surface-variant">Tasks due</p><p className="text-3xl font-bold mt-2">{remaining.length}</p></div><div className="bg-white rounded-xl border border-outline-variant p-5"><p className="text-sm text-on-surface-variant">Meetings today</p><p className="text-3xl font-bold mt-2">6</p></div><div className="bg-white rounded-xl border border-outline-variant p-5"><p className="text-sm text-on-surface-variant">Priority leads</p><p className="text-3xl font-bold mt-2">2</p></div></section><section className="bg-white rounded-xl border border-outline-variant shadow-sm"><div className="p-6 border-b"><h3 className="text-xl font-bold">Prioritized tasks</h3></div>{remaining.length === 0 ? <div className="py-14 text-center"><span className="material-symbols-outlined text-primary text-4xl">task_alt</span><p className="mt-3 font-bold">All caught up for today.</p><p className="text-sm text-on-surface-variant">Your priority tasks are complete.</p></div> : <div className="divide-y">{remaining.map((task) => <div key={task.id} className="p-5 flex items-center gap-4"><span className="material-symbols-outlined text-primary">task</span><div className="flex-1"><p className="font-semibold">{task.title}</p><p className="text-sm text-on-surface-variant">{task.priority} priority · {task.points} reward points</p></div><button onClick={() => completeTask(task.id)} className="px-4 py-2 rounded-lg border border-primary text-primary font-semibold">Mark complete</button></div>)}</div>}</section></section>;
}
