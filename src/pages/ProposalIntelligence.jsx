import { useState } from 'react';
import { Link } from 'react-router-dom';
const products = [['Segregated Fund Portfolio', 'Moderate growth', '87%'], ['Participating Whole Life', 'Estate transfer', '79%'], ['Term Life + CI rider', 'Income protection', '71%']];
export default function ProposalIntelligence() { const [created, setCreated] = useState(false); const [loading, setLoading] = useState(false); const generate = () => { setCreated(false); setLoading(true); window.setTimeout(() => { setLoading(false); setCreated(true); }, 900); }; return <section className="p-8 space-y-6 bg-surface-gray min-h-full"><div><p className="text-primary font-semibold text-sm">Sell</p><h2 className="text-3xl font-bold">Proposal Intelligence</h2><p className="text-on-surface-variant mt-1">Mock recommendations for <Link to="/customer-720?customerId=C-1001" className="text-primary font-semibold hover:underline">Jean-Pierre Tremblay</Link>’s retirement and estate goals.</p></div><div className="bg-primary/10 border border-primary/20 rounded-xl p-5"><b>Needs analysis:</b> Moderate growth appetite, recent liquidity event, retirement income and estate transfer are priorities.</div><div className="grid lg:grid-cols-3 gap-5">{products.map(([name, fit, score]) => <article key={name} className="bg-white rounded-xl border border-outline-variant p-6 shadow-sm"><p className="text-primary text-sm font-semibold">Fit score {score}</p><h3 className="text-xl font-bold mt-2">{name}</h3><p className="text-on-surface-variant mt-3 text-sm">Best for: {fit}</p></article>)}</div><button onClick={generate} disabled={loading} className="bg-primary text-white px-5 py-3 rounded-lg font-semibold disabled:opacity-60">{loading ? 'AI is building proposal…' : 'Generate proposal'}</button>{created && <p className="text-primary font-semibold">Mock proposal created and added to Jean-Pierre’s proposal history.</p>}</section>; }
